<%= form_with model: product, local: true, id: "product-form" do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
    <%= render_error(product, :name) %>
  </div>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
    <%= render_error(product, :name) %>
  </div>
  <%= f.submit "Save Product", class: "btn btn-primary", id: "submit-button" %>
<% end %>
